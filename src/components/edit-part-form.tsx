"use client";
import { useActionState, useEffect, useId } from "react";
import { Check, Save } from "lucide-react";
import { updateItem, type ActionState } from "@/app/actions/items";
import type { Item } from "@/lib/types";
export function EditPartForm({item,onSaved}:{item:Item;onSaved?:()=>void}){const[state,action,pending]=useActionState<ActionState,FormData>(updateItem.bind(null,item.id),{});useEffect(()=>{if(state.ok)onSaved?.();},[state,onSaved]);return <form action={action} className="card p-4 sm:p-6"><input type="hidden" name="updated_at" value={item.updated_at}/><div className="grid gap-5 sm:grid-cols-2"><Field label="WO Number" name="wo_number" defaultValue={item.wo_number||""}/><Field label="Material Number" name="material_number" defaultValue={item.material_number||""}/><div className="sm:col-span-2"><Field label="Material Description" name="material_description" defaultValue={item.material_description||""}/></div><div className="sm:col-span-2"><Field label="Location" name="location" defaultValue={item.location||""}/></div><Field label="Quantity" name="quantity" type="number" min="0" step="1" defaultValue={item.quantity??""}/><div><label className="label" htmlFor={`edit-condition-${item.id}`}>Condition</label><select className="field" name="condition" id={`edit-condition-${item.id}`} defaultValue={item.condition}><option>New</option><option>Good</option><option>Requires Inspection</option></select></div></div>{state.error&&<p role="alert" className="mt-5 rounded-xl bg-red-50 p-3 text-sm text-red-700">{state.error}</p>}{state.ok&&<p role="status" className="mt-5 flex items-center gap-2 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700"><Check className="h-4 w-4"/>Changes saved.</p>}<button disabled={pending} className="btn-primary mt-6 w-full sm:w-auto"><Save className="h-4 w-4"/>{pending?"Saving…":"Save changes"}</button></form>}
function Field({label,...props}:{label:string}&React.InputHTMLAttributes<HTMLInputElement>){const id=useId();return <div><label className="label" htmlFor={id}>{label}</label><input className="field" id={id} {...props}/></div>}
