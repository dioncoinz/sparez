"use client";
import { useActionState } from "react";
import { Check, Save } from "lucide-react";
import { updateItem, type ActionState } from "@/app/actions/items";
import type { Item } from "@/lib/types";
export function EditPartForm({item}:{item:Item}){const[state,action,pending]=useActionState<ActionState,FormData>(updateItem.bind(null,item.id),{});return <form action={action} className="card p-4 sm:p-6"><div className="grid gap-5 sm:grid-cols-2"><Field label="WO Number" name="wo_number" defaultValue={item.wo_number||""}/><Field label="Material Number" name="material_number" defaultValue={item.material_number||""}/><div className="sm:col-span-2"><Field label="Material Description" name="material_description" defaultValue={item.material_description||""}/></div><div className="sm:col-span-2"><Field label="Location" name="location" defaultValue={item.location||""}/></div><Field label="Quantity" name="quantity" type="number" min="0" step="1" defaultValue={item.quantity??""}/><div><label className="label" htmlFor="condition">Condition</label><select className="field" name="condition" id="condition" defaultValue={item.condition}><option>New</option><option>Good</option><option>Requires Inspection</option></select></div><div className="sm:col-span-2"><label className="label" htmlFor="notes">Notes</label><textarea className="field min-h-28 py-3" name="notes" id="notes" defaultValue={item.notes||""}/></div></div>{state.error&&<p className="mt-5 rounded-xl bg-red-50 p-3 text-sm text-red-700">{state.error}</p>}{state.ok&&<p className="mt-5 flex items-center gap-2 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700"><Check className="h-4 w-4"/>Changes saved.</p>}<button disabled={pending} className="btn-primary mt-6 w-full sm:w-auto"><Save className="h-4 w-4"/>{pending?"Saving…":"Save changes"}</button></form>}
function Field({label,...props}:{label:string}&React.InputHTMLAttributes<HTMLInputElement>){return <div><label className="label" htmlFor={String(props.name)}>{label}</label><input className="field" id={String(props.name)} {...props}/></div>}
